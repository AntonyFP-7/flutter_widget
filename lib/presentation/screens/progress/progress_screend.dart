import 'package:flutter/material.dart';

class ProgressScreend extends StatelessWidget {
    static const name = 'progress_screend';
  const ProgressScreend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}